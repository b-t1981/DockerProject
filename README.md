# DockerProject

Projet Docker multi-services : application web Python et base de données PostgreSQL.

## Démarrage rapide

```bash
docker compose up --build
```

L'application est accessible sur http://localhost:8080
